# websocket-client: the Alpaca feed client (feed.py, bridge upstream
# in alpaca_relay.py). websockets: the relay's LOCAL server side only
# (alpaca_relay.py) — not needed if you never run the relay.
websocket-client>=1.6
websockets>=12.0
